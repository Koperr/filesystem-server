### Project used to learn mainly C++, REST Api, libhttpserver and nlohmann-json for the project for Hackathon 2024.
Creates a filesystem on a http server, client uses REST methods to communicate.
Has basic commands like `undo`, `ls`, `pwd`, `cat`, `printlog`, `cd`, `touch`, `mkdri`, `rm`, etc
